apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base application components for email-provider-resend
# This includes all core components needed for the application

# Common namespace for all application resources
namespace: email-provider-resend-system

resources:
  - rbac
  - services/manager
  - services/webhook